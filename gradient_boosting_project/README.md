# Gradient Boosting — Home Credit Default Risk

ML model using gradient boosting (XGBoost/LightGBM) for credit default prediction, optimizing AUC on Kaggle Home Credit data.

## Folder Structure
- `data/` — raw and processed data
- `notebooks/` — Jupyter notebooks
- `models/` — saved model artifacts
- `output/` — results, plots, and reports
